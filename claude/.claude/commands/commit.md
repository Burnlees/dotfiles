write a concise commit message for the unstaged changes to the current branch, make no reference to claude in the commit message. 

ask for approval from the user, then make a commit to the current branch using the created commit message, again make no reference to claude in the commit message.
